{
    "scripts": {
        "builds": "cd frontend && react-scripts build",
        "install-client": "cd frontend && npm install && cd ..",
        "heroku-postbuild": "npm run install-client && npm run builds",
        "server": "cd backend && npm i &&  node server.js",
        "develop": "concurrently --kill-others-on-fail \"npm run server\"",
        "start": "npm run server"
    },
    "dependencies": {
        "axios": "^0.27.2",
        "cors": "^2.8.5",
        "express": "^4.18.1",
        "nodemon": "^2.0.19"
    }
}
